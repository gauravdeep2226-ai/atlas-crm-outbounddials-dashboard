# The Atlas HQ — Call Engine Dashboard
# Copy to `.env` for local dev. In Vercel, set these as Project Environment Variables.
# The repo is private, but secrets still never get committed (.env is gitignored).

VITE_API_URL=https://script.google.com/macros/s/XXXXXXXXXXXXXXXXXXXXXXXX/exec
VITE_API_TOKEN=your_token_here
